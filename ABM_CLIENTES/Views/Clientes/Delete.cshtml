@model ABM_CLIENTES.Models.Cliente

@{
    ViewData["Title"] = "Delete";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h2>Borrrar Cliente</h2>
                </div>
                <div class="card-body">
                    <h3>¿Estas seguro que quéres eliminar a este Cliente?</h3>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Nombre)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Nombre)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Apellido)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Apellido)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Dni)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Dni)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.NumCliente)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.NumCliente)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Domicilio)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Domicilio)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.FechaRegistro)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.FechaRegistro)</dd>
                    </dl>
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="IdCliente" />
                        <div class="form-group">
                            <input type="submit" value="Delete" class="btn btn-danger" />
                            <a asp-action="Index" class="btn btn-secondary">Volver a la lista</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}